[profile.default]
src = "contracts"
test = "test/invariants"
out = "out"
libs = ["lib"]
fs_permissions = [{ access = "read-write", path = "./" }]

[profile.sandbox]
src = "sandbox/contracts"
test = "test"
out = "out"
libs = ["lib"]
fs_permissions = [{ access = "read-write", path = "./" }]
